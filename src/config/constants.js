const ROOM_NAME = {
	userRoom: 'users room',
}

const USER_STATUS = {
	online: 'online',
	offline: 'offline',
	busy: 'busy',
};

const USER_MESSAGES = { 
	doctorBusy: 'doctor is busy',
}

const ERROR_MESSAGE = {
	cannotVerifyRoom: 'cannot verify room',
	userNotFound: 'user not found',
};

module.exports = {
	ROOM_NAME,
	USER_STATUS,
	USER_MESSAGES,
	ERROR_MESSAGE,
}